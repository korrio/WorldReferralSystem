# =================================
# World Referral System Environment Variables
# =================================

# Database Configuration
# For local development with Docker: postgresql://postgres:password123@db:5432/worldref
# For local development without Docker: postgresql://postgres:password123@localhost:5432/worldref
# For production: Will be provided by your hosting service (e.g., Render.com, Railway, etc.)
DATABASE_URL=postgresql://postgres:password123@localhost:5432/worldref

# Session Configuration
# IMPORTANT: Change this in production - use a strong random string
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# World ID Configuration
# Get these values from https://developer.worldcoin.org/
# VITE_ prefix is required for Vite to expose to client-side code
VITE_WORLD_ID_APP_ID=app_staging_7dd00a92b79e4165684dcf3c65d474a9

# Application Configuration
NODE_ENV=development
# Port for local development (Docker uses 8000, local can use any available port)
PORT=8000

# =================================
# Docker-specific Configuration
# =================================
# When using Docker Compose, the database host should be 'db' instead of 'localhost'
# Docker Compose will automatically use these environment variables
# Example for Docker: DATABASE_URL=postgresql://postgres:password123@db:5432/worldref

# =================================
# Production Deployment Notes
# =================================
# For Render.com:
# - DATABASE_URL: Will be auto-provided by PostgreSQL service
# - SESSION_SECRET: Will be auto-generated 
# - NODE_ENV: Set to "production"
# - PORT: Will be auto-set by Render (usually 10000)
# - VITE_WORLD_ID_APP_ID: Set your production World ID app ID

# For other hosting services:
# - Ensure DATABASE_URL points to your PostgreSQL instance
# - Set NODE_ENV=production
# - Use strong SESSION_SECRET
# - Set appropriate PORT (check your hosting service requirements)